# 開発ログ

新しい順に追記。日付は YYYY-MM-DD。

---

## 2026-05-07

### プロダクト再定義：「AI カメラマン」

このアプリは「ポーズ提案アプリ」ではなく **「AI カメラマン」** として撮影ディレクションを代行するプロダクト、と明確に再定義。提案するのは「ポーズ」ではなく **「状態（姿勢 + 構図）」**。詳細は [design.md](design.md)。

### 確定した設計事項

- **盛れる = 状態の最適化**：顔角度・体の傾き・手の余白・カメラ距離・背景関係。「腰に手を当てる」ではなく「顔が小さく見える角度 + 立体感 + 余白調整」
- **状態テンプレート（StateTemplate）**：完成した写真のスナップショットを構造化データで保持。`subject` / `composition` / `background_hint` の 3 ブロック。`background_hint` は MVP では未使用、フィールドのみ予約
- **テンプレ作成方針**：P1 は手作り 5 個（自分 or 友人で撮影 → MediaPipe で関節抽出 → 手で微調整）。P2 でコーパス収集、P3 で半自動生成
- **シルエット**：半透明人体 + 関節マーカーのハイブリッド（スティックは抽象すぎ、完全人体は合わせづらい）
- **補間**：最近傍テンプレへ 0.1〜0.2 秒で線形補間。ヒステリシスで切り替え抑制
- **セッション**：1 起動 = 5〜10 枚。同じテンプレを連続選択しない。フォルダ化保存
- **失敗を見せない**：スコアが上がらない時は近傍テンプレに気づかれず切り替え
- **起動体験**：完全無言開始。0.2 秒以内に人物検出 → シルエット出現 → 視覚フィードバック開始
- **オンボーディング**：初回のみ 3 秒、「かざすだけで、自動でいい写真を撮ります」、タップで消える
- **指示優先順位**：視覚 > 触覚 > 音声（音声はデフォルト OFF、設定で ON）
- **初期ターゲット**：他撮り（撮影者が画面、被写体が指示を受ける）
- **良い顔判定**：MVP では「顔検出 + 目開き + 角度範囲内」のみ。笑顔判定は後回し
- **残す操作**：ピンチズーム、端末回転、フラッシュ。**隠す**：モード切替、ポーズ選択、手動シャッター
- **将来候補**：Apple Watch 連携での被写体振動指示（公共空間での音声代替）

### ドキュメント整備

- 新規 `docs/design.md`：状態テンプレートのスキーマ、PoseMatcher、補間、セッション、UX 原則を集約。実装中に常に参照する文書として独立
- `CLAUDE.md` 全面改訂：AI カメラマン再定義、コアコンセプト、フィードバック優先順位、操作方針を反映
- `docs/roadmap.md`：P1 タスクを 9 セクションに細分化（カメラ基盤・ビジョン統合・状態テンプレート・シルエット補間・スコア・フィードバック・自動シャッター・セッション・オンボーディング）
- `docs/stack.md`：状態テンプレートのデータ構造、Apple Watch 連携、PhotoKit、Core Haptics 等を追記

### 開発フローの確定：「体験 → AI」の順

ロードマップを「体験プロトタイプ → MVP（非 AI）→ AI 導入 → コア機能 → UX 改善 → 撮影体験拡張 → リリース」の 7 フェーズに再構成。

- **P0：体験プロトタイプ（最重要）** — AI なし、ダミーで「AI カメラマンっぽさ」を検証。**1 日で終わらせる**
- **P1：MVP（非 AI）** — 実カメラ + ダミー PoseEstimator で UX を完成させる
- **P2：AI 導入** — MediaPipe / Vision でダミーを実データに置換

判断基準：**「ここで面白くなければ、AI を入れても面白くならない」**。先に体験の骨格を確定させ、AI は後から差し込む。技術ではなく体験を作るプロジェクトとして開発する。

`CLAUDE.md` に開発方針セクションを追記。`docs/roadmap.md` を全面改訂。

### 次のステップ

- **P0 体験プロトタイプの実装に着手**（カメラ風 UI・仮シルエット・ダミースコア・自動シャッター演出・擬似保存）
- 並行して MediaPipe iOS 統合方法の調査
- 状態テンプレート 5 個の手作りは P3 で着手

### 設計の核（忘れないために）

> このプロダクトの本質は「モデル」ではなく **「良さの定義」**。
> 最初は開発者の感覚をそのままコードに写す。
>
> 技術ではなく **体験を作るプロジェクト**。

---

## 2026-05-05

### プロジェクト初期セットアップ

- Xcode テンプレートから `kime` プロジェクトを作成（iOS 26.4、Swift 5、SwiftUI）
- ファイル構成を再設計：`App/ Features/ Core/ Data/ Resources/ Supporting/` の 6 ディレクトリ
  - `Core/` 配下に `Camera/Vision/Domain/Guidance/` を分割
  - 各レイヤーに placeholder Swift ファイルを配置
- `ContentView.swift` → `App/AppRoot.swift` にリネーム
- `project.pbxproj` を更新し `PBXFileSystemSynchronizedRootGroup` で 4 つの新ディレクトリを登録
- `.gitignore` を追加（`.DS_Store`、`xcuserdata/`、`DerivedData/` 等）
- `xcodebuild` でビルド成功を確認
- `CameraViewModel` は `@Observable`（iOS 17+）で実装する方針に決定
- `docs/` ディレクトリを作成し `CLAUDE.md`、`devlog.md`、`stack.md`、`roadmap.md` を整備

### 決定事項

- ディレクトリ構成は `src/` でラップしない（iOS 慣習に従い直下配置）
- 非ソースのファイルは将来 `docs/`、`scripts/`、`design/`、`ml/` 等を兄弟ディレクトリとして追加していく
- テストターゲットは未追加（必要になったタイミングで Xcode から追加する）

### 次のステップ

- P1 MVP の着手（カメラプレビュー → MediaPipe 統合 → ポーズ判定 → 自動シャッター）
- 詳細は [roadmap.md](roadmap.md) 参照
