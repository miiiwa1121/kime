# kime

**「AI カメラマン」** として撮影ディレクションを代行する iOS カメラアプリ。

カメラを向けるだけで、被写体の **状態（姿勢 + 構図）** を評価し、シルエット・テキスト・触覚で誘導、条件達成で自動シャッター。「ポーズアプリ」ではなく **「写真体験の OS」** として設計する。

初期は他撮りを優先する（撮影者が画面を見て、被写体が指示を受ける構図）。

## コアコンセプト

- **状態テンプレート**：完成した写真のスナップショット（顔角度・体の傾き・手の余白・カメラ距離）の集合。関節座標の集合ではなく「写真として完成した状態」
- **失敗が存在しない**：スコアが上がらない時はユーザーに気づかれず近傍テンプレートに切り替え、シルエットを補間して滑らかに変化させる
- **AI カメラマン体験**：1 起動 = 5〜10 枚のセッション。連続して構図が切り替わり「撮ってもらってる感」を出す

## 開発方針

**体験 → AI の順で作る**。最初は「それっぽさ」をダミーで作り、UX が成立してから AI に置き換える。技術ではなく **体験を作るプロジェクト**。

開発フローは [docs/roadmap.md](docs/roadmap.md) 参照（P0 体験プロトタイプ → P1 MVP 非 AI → P2 AI 導入 → ...）。

## 設計思想（必ず守る）

- **ゼロクリック徹底** — 起動 → 検出 → ガイド出現を 0.2 秒以内、無言で開始。「今から提案します」の間を作らない
- **状態は頻繁に切り替えない** — テンプレート間の補間は 0.1〜0.2 秒。揺らぎを抑制
- **指示は 1〜2 個まで** — 褒めベース（ポジティブ）。「あごちょっと引いて」のような具体・短文
- **リアルタイム処理は軽量に** — 推定は 15fps に間引く。プレビューは 30fps 以上維持
- **オンデバイス完結** — MVP 段階ではネットワーク依存禁止
- **正確さより気持ちよさ、多機能より一貫性**

## ステートマシン

`SEARCH → GUIDE → CAPTURE` の 3 状態でユーザー体験を制御する（P2 で実装）。

## フィードバックの優先順位

1. **視覚**（最重要）：シルエット + 短いテキスト
2. **触覚**：撮影瞬間の振動
3. **音声**：デフォルト OFF。設定で ON 可。将来 Apple Watch 連携も候補

## 残す/隠す操作

| 残す | 隠す |
|---|---|
| ピンチズーム | モード切替 |
| 端末回転 | ポーズ選択 |
| フラッシュ設定 | 手動シャッター（基本なし） |

長押しで次候補に切り替える隠しジェスチャは P2 以降で検討。

## ディレクトリ構成

```
App/                  エントリポイント（KimeApp, AppRoot）
Features/             画面単位（Camera/ など）
Core/
  Camera/             AVFoundation ラッパー
  Vision/             MediaPipe + Vision（姿勢・顔）
  Domain/             モデル（StateTemplate, PoseScore, CaptureState, CaptureSession）
  Guidance/           ディレクションロジック（Matcher, Recommender, Feedback, AutoShutter, Interpolator）
Data/                 状態テンプレート DB（JSON）と Repository
Resources/            Assets.xcassets
Supporting/           Info.plist 等
docs/                 開発ログ・技術スタック・ロードマップ・設計詳細
```

ロジック層（`Core/`）は UI 非依存。テスト可能性を確保する。

## 技術スタック

Swift / SwiftUI / AVFoundation / MediaPipe（Pose）/ Vision（顔）。詳細は [docs/stack.md](docs/stack.md)。

## 関連ドキュメント

- [docs/design.md](docs/design.md) — **状態テンプレート設計、補間、セッション、UX 原則の詳細（実装中に常に参照）**
- [docs/roadmap.md](docs/roadmap.md) — フェーズ、タスク、将来像
- [docs/devlog.md](docs/devlog.md) — 開発ログ
- [docs/stack.md](docs/stack.md) — 技術選定の詳細

## 開発時の注意

- スコア閾値は厳しすぎないこと（撮影成立を優先）
- 重い AI 処理は同期実行しない
- 状態テンプレートを毎フレーム変更しない（補間で繋ぐ）
- UI を複雑にしない
- "失敗" をユーザーに見せない（ダメな時はテンプレを微調整して気づかせない）
- このプロダクトの本質は **「良さの定義」をコードに写すこと**。最初は開発者の感覚をそのままテンプレートに反映する
